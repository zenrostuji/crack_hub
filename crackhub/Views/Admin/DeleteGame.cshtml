@model crackhub.Models.Data.Game
@{
    ViewData["Title"] = "Xóa Game";
    Layout = "_AdminLayout";
}

<div class="admin-delete-game">
    <div class="admin-page-title-container mb-4">
        <h4 class="admin-page-title">
            <i class="fas fa-trash me-2"></i> Xóa game: @Model.Title
        </h4>
        <a href="@Url.Action("Games", "Admin")" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i> Quay lại
        </a>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="admin-form-section">
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Cảnh báo!</strong> Bạn có chắc chắn muốn xóa game này không? Hành động này không thể hoàn tác và tất cả dữ liệu liên quan đến game sẽ bị xóa.
                </div>

                <div class="p-4 rounded bg-light mb-4">
                    <div class="row mb-3">
                        <div class="col-md-4 text-muted">ID:</div>
                        <div class="col-md-8 fw-bold">@Model.Id</div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-4 text-muted">Tên game:</div>
                        <div class="col-md-8 fw-bold">@Model.Title</div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-4 text-muted">Thể loại:</div>
                        <div class="col-md-8">@(Model.Category?.CategoryName ?? "Không có")</div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-4 text-muted">Nhà phát triển:</div>
                        <div class="col-md-8">@(Model.Developer ?? "Không có")</div>
                    </div>
                    <div class="row">
                        <div class="col-md-4 text-muted">Lượt tải:</div>
                        <div class="col-md-8">@Model.Downloads</div>
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a href="@Url.Action("Games", "Admin")" class="btn btn-secondary">
                        <i class="fas fa-times me-2"></i> Hủy
                    </a>
                    <form method="post" asp-action="DeleteGame" asp-controller="Admin" asp-route-id="@Model.Id">
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash me-2"></i> Xác nhận xóa
                        </button>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="col-lg-4">
            <div class="admin-form-section">
                <div class="game-info-card">
                    <div class="game-cover">
                        <img src="@(string.IsNullOrEmpty(Model.CoverImageUrl) ? "/img/no-image.jpg" : Model.CoverImageUrl)" 
                             alt="@Model.Title" class="img-fluid" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>